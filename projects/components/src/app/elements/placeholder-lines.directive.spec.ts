import { PlaceholderLinesDirective } from './placeholder-lines.directive';

describe('PlaceholderLinesDirective', () => {
  it('should create an instance', () => {
    const directive = new PlaceholderLinesDirective();
    expect(directive).toBeTruthy();
  });
});
